{
  "name": "telex-taskkeeper-agent",
  "version": "0.1.0",
  "description": "TaskKeeper Telex A2A agent (file-backed storage). Pure agent (stdin/stdout) implementation in TypeScript.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist && rm -rf data"
  },
  "files": [
    "dist",
    "src",
    "data"
  ],
  "dependencies": {
    "body-parser": "^1.20.2",
    "chrono-node": "^2.3.2",
    "dayjs": "^1.11.13",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "@types/uuid": "^9.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "license": "MIT"
}
